<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- WHY: voice prompting. Android only lets an app ASK the user for the
         microphone at runtime if the app also declares it here up front; without
         this line the request silently fails and the voice button is dead. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- WHY: declaring RECORD_AUDIO makes Google Play assume the app REQUIRES a
         microphone and hide it from any device without one. Saying required=false
         keeps YouCoded installable everywhere, exactly as the camera line above
         does for the QR scanner. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <queries>
        <package android:name="com.tailscale.ipn" />
        <!-- WHY: since Android 11, an app cannot even SEE other apps on the phone
             unless it names them here. The speech recogniser is another app, so
             without this entry SpeechRecognizer.isRecognitionAvailable() returns
             false on every phone and voice prompting looks unsupported everywhere.
             We target SDK 35, so the restriction definitely applies to us. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- configChanges: keep this list maximal. Without it, every soft-keyboard
             show/hide, rotation, dark-mode toggle, or multi-window resize destroys
             MainActivity, which destroys the WebView (wiping React chat state and
             active-session tracking) and re-runs initBootstrap (which re-installs
             bundled plugins → /reload-plugins gets typed into the live session).
             Process death from memory reclaim still recreates the Activity and is
             handled separately by a state-rehydration path. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|navigation|uiMode|locale|fontScale|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="youcoded" android:host="skill" />
                <data android:scheme="youcoded" android:host="plugin" />
            </intent-filter>
        </activity>

        <service
            android:name=".runtime.SessionService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

    </application>
</manifest>
