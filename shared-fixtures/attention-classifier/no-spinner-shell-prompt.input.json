{
  "description": "Shell prompt in buffer with no spinner → unknown (shell-idle escalation is time-based in the hook, not classifyBuffer)",
  "bufferTail": [
    "user@hostname:~/project$ npm run build",
    "> youcoded@1.0.0 build",
    "> vite build",
    "user@hostname:~/project$"
  ],
  "previousSpinnerSeconds": null,
  "secondsSincePreviousSpinner": 0
}
