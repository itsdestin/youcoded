// marketplace-constants.ts
// Shared character-limit constants for marketplace text inputs.
// Centralised here so RatingSubmitModal and ReportReviewButton stay in sync.

/** Maximum characters for a review body (rating textarea). */
export const REVIEW_TEXT_MAX = 500;

/** Maximum characters for a report reason textarea. */
export const REPORT_REASON_MAX = 300;
