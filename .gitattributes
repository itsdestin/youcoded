# Force LF line endings for scripts that run on Android/Linux
*.sh text eol=lf
*.js text eol=lf
gradlew text eol=lf

# Windows batch files should keep CRLF
*.bat text eol=crlf

# Default: auto-detect
* text=auto
